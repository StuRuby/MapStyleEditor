import React from 'react';
import Modal from '../../components/modals/Modal';

export default function ExportModal(props) {
    return (
        <Modal>
            <div>This is a ExportModal</div>
        </Modal>
    );
}