import React from 'react';
import Modal from '../../components/modals/Modal';

export default function SettingModal(props) {
    return (
        <Modal>
            <div>This is a SettingModal</div>
        </Modal>
    );
}