import React from 'react';
import Modal from '../../components/modals/Modal';

export default function LoadingModal(props) {
    return (
        <Modal>
            <div>This is a LoadingModal</div>
        </Modal>
    );
}