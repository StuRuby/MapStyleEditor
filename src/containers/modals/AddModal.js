import React from 'react';
import Modal from '../../components/modals/Modal';

export default function AddModal(props) {
    return (
        <Modal>
            <div>This is a AddModal</div>
        </Modal>
    );
}