import React from 'react';
import Modal from '../../components/modals/Modal';

export default function OpenModal(props) {
    return (
        <Modal>
            <div>This is a OpenModal</div>
        </Modal>
    );
}